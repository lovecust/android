apply plugin: 'com.android.application'
apply plugin: 'me.tatarka.retrolambda'

// Create a variable called keystorePropertiesFile, and initialize it to your
// keystore.properties file, in the rootProject folder.
def mKeystorePropertiesFile = rootProject.file("keystore.properties")

// Initialize a new Properties() object called keystoreProperties.
def mKeystoreProperties = new Properties()

// Load your keystore.properties file into the keystoreProperties object.
mKeystoreProperties.load(new FileInputStream(mKeystorePropertiesFile))


android {
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
    signingConfigs {
        SigingLovecust {
            keyAlias mKeystoreProperties['keyAlias']
            keyPassword mKeystoreProperties['keyPassword']
            storeFile file(mKeystoreProperties['storeFile'])
            storePassword mKeystoreProperties['storePassword']
        }
    }
    compileSdkVersion 24
    buildToolsVersion '24.0.2'
    defaultConfig {
        applicationId 'com.lovecust.default'
        minSdkVersion 15
        targetSdkVersion 24
        versionName 'v0.0.0'
        signingConfig signingConfigs.SigingLovecust
        versionCode 0
        // Enable multi dex to use more methods than 65535.
        // multiDexEnabled true
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.SigingLovecust
        }
        debug {
            signingConfig signingConfigs.SigingLovecust
        }
        BuildTypeLovecust {
            debuggable true
            jniDebuggable false
            signingConfig signingConfigs.SigingLovecust
            renderscriptDebuggable false
            minifyEnabled false
            zipAlignEnabled true
        }
    }
    productFlavors {
        LovecustDebug {
            minSdkVersion 15
            applicationId 'com.lovecust.debug'
            targetSdkVersion 24
            versionName 'v0.8.6'
            versionCode 86
            signingConfig signingConfigs.SigingLovecust
        }
        LovecustRelease {
            minSdkVersion 15
            applicationId 'com.lovecust.app'
            targetSdkVersion 24
            versionName 'v0.9.6'
            versionCode 96
            signingConfig signingConfigs.SigingLovecust
        }
        LovecustTest {
            minSdkVersion 15
            applicationId 'com.lovecust.test'
            targetSdkVersion 24
            versionName 'v0.7.6'
            versionCode 76
            signingConfig signingConfigs.SigingLovecust
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    // Use FisherUtilsLib library.
    compile project(':FisherUtilsLib')
    // Use LovecustNetworkLib library.
    compile project(':LovecustNetworkLib')
    // Use EcustWifiLib module.
    compile project(':EcustWifiLib')
    compile 'com.android.support:cardview-v7:24.2.1'
    compile 'com.android.support:recyclerview-v7:24.2.1'
    compile 'com.android.support:appcompat-v7:24.2.1'
    compile 'com.android.support:design:24.2.1'
    compile 'com.google.code.gson:gson:2.4'
    compile 'io.reactivex:rxjava:1.0.14'
    compile 'io.reactivex:rxandroid:1.0.1'
    compile 'com.trello:rxlifecycle:0.4.0'
    compile 'com.trello:rxlifecycle-components:0.4.0'
    // Use retrofit to fetch internet resources.
    compile 'com.squareup.retrofit2:retrofit:2.0.2'
    // Use gson converter to convert json to object.
    compile 'com.squareup.retrofit2:converter-gson:2.0.2'
    // Support rxJava for retrofit.
    compile 'com.squareup.retrofit2:adapter-rxjava:2.0.2'
    compile 'com.squareup.okhttp:okhttp-urlconnection:2.4.0'
    compile 'com.squareup.okhttp:okhttp:2.4.0'
    compile 'org.litepal.android:core:1.3.1'
    compile 'com.squareup.picasso:picasso:2.5.2'
    compile 'com.jakewharton:butterknife:7.0.1'
    compile 'de.greenrobot:eventbus:2.4.0'
    compile 'me.imid.swipebacklayout.lib:library:1.0.0'
    compile 'org.jsoup:jsoup:1.8.2'
    compile 'cz.msebera.android:httpclient:4.4.1.2'
    // For debug usage; By Fisher at 2016-09-05 22:15;
    // Size: 4,188B; APK-Before-Size: 4,069,766B; APK-After-Size: 4,073,954B;
    // Removed at 08:04 on 2017-02-20. Size: -2,936B; Before-APK-Size: 4,172,649B; After-APK-Size: 4,169,713B;
    // compile 'com.orhanobut:logger:1.15'
}
